spring:
  application:
    name: ribbon-consumer
    
server:
  port: 8874

 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
feign:
  hystrix:
    enabled: true

myname: dengqifeng